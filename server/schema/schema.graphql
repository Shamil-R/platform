type User {
    id: Int!
    name: String!
    roles: [RoleObject!]!
    materials: [Material!]!
}

type RoleObject {
    id: Int!
    object: String!
    access: RoleAccess!
}

enum RoleAccess {
    CREATE
    READ
    UPDATE
    DELETE
}

type Material {
    id: Int!
    name: String!
}
