directive @default(
  value: String
) on FIELD_DEFINITION

type User {
    id: Int!
    name: String!
    roles: [RoleObject!]!
    materials: [Material!]!
}

type RoleObject {
    id: Int!
    object: String!
    access: RoleAccess!
}

type Material {
    id: Int!
    name: String! @validate(min: 0, max: 10)
}

"""
Уровни доступа
"""
enum RoleAccess {
    """
    Создание
    """
    CREATE
    """
    Чтение
    """
    READ
    UPDATE
    DELETE
}